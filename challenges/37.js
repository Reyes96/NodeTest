// Dado un array de números en orden aleatorio, determinar cuál es el valor
// más pequeño.

const { read, readNumber, print } = require('../helpers')

const array = [230, 999, 112, 666, 190, 725, 283, 670]

/*let valorMasPequeno = array[0]; // Suponemos que el primer elemento es el más pequeño

  for (let i = 1; i < array.length; i++) {
    if (array[i] < valorMasPequeno) {
      valorMasPequeno = array[i]; // Actualizamos el valor más pequeño si encontramos uno menor
    }
  }
*/

const main = async () => {
  
  print(`El valor más pequeño en el array es:);
};

module.exports = main
